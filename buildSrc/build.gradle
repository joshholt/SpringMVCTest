apply plugin: 'groovy'

repositories {
    mavenLocal()
    mavenCentral()
    maven { url "http://repo.spring.io/libs-release" }
    jcenter()
}

dependencies {
    compile("org.ajoberstar:gradle-git:0.9.0")
}